syntax = "proto3";

package com.teddy.api.content;

option go_package = "proto";

import "github.com/micro/go-api/proto/api.proto";

service Content {
	rpc Inbox(go.api.Request) returns (go.api.Response) {}
	rpc Notify(go.api.Request) returns (go.api.Response) {}
}
