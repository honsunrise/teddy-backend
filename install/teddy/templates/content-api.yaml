{{- define "teddy.api.content.name" -}}
  {{- printf "%s-%s" (include "teddy.name" .) "api-content" -}}
{{- end -}}

{{- define "teddy.api.content.fullname" -}}
  {{- printf "%s-%s" (include "teddy.fullname" .) "api-content" -}}
{{- end -}}

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "teddy.api.content.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.api.content.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "teddy.api.content.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.api.content.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ default .Values.api.content.type "ClusterIP" }}
    ports:
    - port: {{ default .Values.api.content.port 8080 }}
      targetPort: http
      protocol: TCP
      name: http
    selector:
      app.kubernetes.io/name: {{ include "teddy.api.content.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "teddy.api.content.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.api.content.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ default .Values.api.content.replicas 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "teddy.api.content.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "teddy.api.content.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ include "teddy.api.content.fullname" . }}
      containers:
        - name: {{ include "teddy.api.content.name" . }}
          image: "{{ .Values.api.content.image.repository }}:{{ .Values.api.content.image.tag }}"
          imagePullPolicy: {{ .Values.api.content.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
{{ toYaml .Values.resources | indent 12 }}
{{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
{{- end }}