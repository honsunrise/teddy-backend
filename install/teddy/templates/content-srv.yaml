{{- define "teddy.services.content.name" -}}
  {{- printf "%s-%s" (include "teddy.name" .) "service-content" -}}
{{- end -}}

{{- define "teddy.services.content.fullname" -}}
  {{- printf "%s-%s" (include "teddy.fullname" .) "service-content" -}}
{{- end -}}

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "teddy.services.content.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.services.content.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "teddy.services.content.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.services.content.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ default "ClusterIP" .Values.services.content.type }}
    ports:
    - port: {{ default 8080 .Values.services.content.port }}
      targetPort: http
      protocol: TCP
      name: http
    selector:
      app.kubernetes.io/name: {{ include "teddy.services.content.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "teddy.services.content.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.services.content.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ default 1 .Values.services.content.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "teddy.services.content.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "teddy.services.content.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ include "teddy.services.content.fullname" . }}
      containers:
        - name: {{ include "teddy.services.content.name" . }}
          image: "{{ .Values.services.content.image.repository }}:{{ .Values.services.content.image.tag }}"
          imagePullPolicy: {{ .Values.services.content.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
{{ toYaml .Values.resources | indent 12 }}
{{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
{{- end }}