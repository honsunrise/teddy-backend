{{- define "teddy.services.uaa.name" -}}
  {{- printf "%s-%s" (include "teddy.name" .) "service-uaa" -}}
{{- end -}}

{{- define "teddy.services.uaa.fullname" -}}
  {{- printf "%s-%s" (include "teddy.fullname" .) "service-uaa" -}}
{{- end -}}

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "teddy.services.uaa.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.services.uaa.name" . }}
    app.kubernetes.io/version: {{ .Values.services.uaa.version }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "teddy.services.uaa.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.services.uaa.name" . }}
    app.kubernetes.io/version: {{ .Values.services.uaa.version }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ default "ClusterIP" .Values.services.uaa.type }}
    ports:
    - port: {{ default 8080 .Values.services.uaa.port }}
      targetPort: http
      protocol: TCP
      name: http
    selector:
      app.kubernetes.io/name: {{ include "teddy.services.uaa.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "teddy.services.uaa.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.services.uaa.name" . }}
    app.kubernetes.io/version: {{ .Values.services.uaa.version }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ default 1 .Values.services.uaa.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "teddy.services.uaa.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "teddy.services.uaa.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ include "teddy.services.uaa.fullname" . }}
      containers:
        - name: {{ include "teddy.services.uaa.name" . }}
          image: "{{ .Values.services.uaa.image.repository }}:{{ .Values.services.uaa.image.tag }}"
          imagePullPolicy: {{ .Values.services.uaa.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
{{ toYaml .Values.resources | indent 12 }}
{{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
{{- end }}