{{- define "teddy.apis.base.name" -}}
  {{- printf "%s-%s" (include "teddy.name" .) "api-base" -}}
{{- end -}}

{{- define "teddy.apis.base.fullname" -}}
  {{- printf "%s-%s" (include "teddy.fullname" .) "api-base" -}}
{{- end -}}

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "teddy.apis.base.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.apis.base.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "teddy.apis.base.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.apis.base.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ default "ClusterIP" .Values.apis.base.type }}
    ports:
    - port: {{ default 8080 .Values.apis.base.port }}
      targetPort: http
      protocol: TCP
      name: http
    selector:
      app.kubernetes.io/name: {{ include "teddy.apis.base.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "teddy.apis.base.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.apis.base.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ default 1 .Values.apis.base.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "teddy.apis.base.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "teddy.apis.base.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ include "teddy.apis.base.fullname" . }}
      containers:
        - name: {{ include "teddy.apis.base.name" . }}
          image: "{{ .Values.apis.base.image.repository }}:{{ .Values.apis.base.image.tag }}"
          imagePullPolicy: {{ .Values.apis.base.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
{{ toYaml .Values.resources | indent 12 }}
{{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
{{- end }}