{{- define "teddy.api.message.name" -}}
  {{- printf "%s-%s" (include "teddy.name" .) "api-message" -}}
{{- end -}}

{{- define "teddy.api.message.fullname" -}}
  {{- printf "%s-%s" (include "teddy.fullname" .) "api-message" -}}
{{- end -}}

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "teddy.api.message.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.api.message.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "teddy.api.message.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.api.message.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ default .Values.api.message.type "ClusterIP" }}
    ports:
    - port: {{ default .Values.api.message.port 8080 }}
      targetPort: http
      protocol: TCP
      name: http
    selector:
      app.kubernetes.io/name: {{ include "teddy.api.message.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "teddy.api.message.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "teddy.api.message.name" . }}
    helm.sh/chart: {{ include "teddy.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ default .Values.api.message.replicas 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "teddy.api.message.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "teddy.api.message.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ include "teddy.api.message.fullname" . }}
      containers:
        - name: {{ include "teddy.api.message.name" . }}
          image: "{{ .Values.api.message.image.repository }}:{{ .Values.api.message.image.tag }}"
          imagePullPolicy: {{ .Values.api.message.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
{{ toYaml .Values.resources | indent 12 }}
{{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
{{- end }}